version: '3.8'

volumes:
  n8n_data:

services:
  n8n:
    image: docker.n8n.io/n8nio/n8n
    restart: always
    ports:
      - "5678:5678"
    environment:
      - GENERIC_TIMEZONE=America/Caracas
      - WEBHOOK_URL=https://9a737e246af601.lhr.life/
    volumes:
      - n8n_data:/home/node/.n8n